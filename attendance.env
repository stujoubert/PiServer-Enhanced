# PiServer Attendance System - Environment Configuration
# Copy this file to /etc/attendance/attendance.env and customize

# Environment (prod, dev, or test)
ATT_ENV=prod

# Database location
ATT_DB=/var/lib/attendance/attendance.db

# Server port
ATT_PORT=5000

# Secret key for sessions (CHANGE THIS!)
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-secret-key

# Timezone (see https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=UTC

# Optional: Redis for caching (if available)
# REDIS_URL=redis://localhost:6379/0

# Optional: Email notifications (if configured)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourcompany.com

# Optional: API settings
# API_RATE_LIMIT=100 per hour
