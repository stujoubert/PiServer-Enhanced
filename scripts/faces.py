def push_face(
    device,
    employee_id,
    image_path
):
    """
    device: object or dict with ip, username, password
    employee_id: str/int
    image_path: absolute path to jpg
    """
    ...
